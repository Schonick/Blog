@model SportClub.Models.Discounts

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Discounts</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.discounts)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.discounts)
            @Html.ValidationMessageFor(model => model.discounts)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PhytobarProductsID, "PhytobarProduct")
        </div>
        <div class="editor-field">
            @Html.DropDownList("PhytobarProductsID", String.Empty)
            @Html.ValidationMessageFor(model => model.PhytobarProductsID)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
