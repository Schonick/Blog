@model SportClub.Models.Discounts

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Discounts</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.discounts)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.discounts)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.PhytobarProduct.Products)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PhytobarProduct.Products)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
